import "package:flutter/material.dart";

class PostScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Center(child: Text('Đây là màn hình Post'))
    );
  }
}
